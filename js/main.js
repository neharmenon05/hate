import './auth.js';

// Hide loading screen initially and show it
document.addEventListener('DOMContentLoaded', () => {
    // The auth manager will handle showing/hiding the loading screen
    setTimeout(() => {
        // This timeout ensures smooth loading transition
    }, 100);
});